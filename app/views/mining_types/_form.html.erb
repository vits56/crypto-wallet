<%= form_with(model: mining_type, local: true) do |form| %>
  <% if mining_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mining_type.errors.count, "error") %> prohibited this mining_type from being saved:</h2>

      <ul>
      <% mining_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group text-center">
    <h2><%= form.label :description %></h2>
    <%= form.text_field :description, class:"form-control text-center", placeholder:"Proof of Work" %>
  </div>

  <div class="form-group text-center">
    <h2><%= form.label :acronym %></h2>
    <%= form.text_field :acronym, class:"form-control text-center", placeholder:"Pow" %>
  </div>
  <br>
  <div class="actions text-center">
    <%= form.submit nil, class:"btn btn-primary" %>
  </div>
<% end %>

</br>
